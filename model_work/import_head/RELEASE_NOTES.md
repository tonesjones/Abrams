Pre-import-head snapshot. Working bind (2026-08-12) plus the dropped sully.glb.

These two Blender files are the restore point if a later head pass breaks the bind:

- sully_skinned.blend — last successful skinned export
- sully_cut.blend — cut Abrams body + primitive head

The current source and previews live under `model_work/import_head/`; the V3
donor experiment is archived under `model_work/head_donor/`. Scripts and VPKs
are kept at the repository root and under `model_work/fullbody/`.
